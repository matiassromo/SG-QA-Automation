import test from 'node:test';import assert from 'node:assert/strict';import { emptyAutomationSettings,settingsContext,validateAutomationSettings } from './automation-settings.ts';
test('una configuración vacía nunca se considera lista',()=>{assert.deepEqual(settingsContext(emptyAutomationSettings('A')),{baseUrl:false,testUser:false,authContext:false,navigationContext:false});});
test('las credenciales se referencian por nombres de variables y no por secretos',()=>{const value={...emptyAutomationSettings('A'),baseUrl:'https://qa.example.test',usernameEnv:'QA_USER',passwordEnv:'QA_PASSWORD',usernameLocator:'[name=email]',passwordLocator:'[name=password]',submitLocator:'button[type=submit]',authenticatedLocator:'[data-testid=home]',navigationLocator:'nav'};assert.deepEqual(validateAutomationSettings(value),[]);assert.deepEqual(settingsContext(value),{baseUrl:true,testUser:true,authContext:true,navigationContext:true});});
test('rechaza una URL y nombres de entorno inseguros',()=>{const value={...emptyAutomationSettings('A'),baseUrl:'javascript:alert(1)',usernameEnv:'usuario real',passwordEnv:'secret'};assert.ok(validateAutomationSettings(value).length>=3);});
